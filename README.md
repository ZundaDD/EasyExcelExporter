# EasyExcelExporter

A very simple and easy excel exporter.
Just donwload .exe in release and right click, it will scan excels and generate `config.json` for you to customize.
Then use `.\ExcelExporter.exe xxx` (xxx is the file name of excel without extension) to export scripts and jsons.
